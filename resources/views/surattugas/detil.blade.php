<a href="#" data-toggle="modal" data-target="#ViewModal" data-srtid="{{$item->srt_id}}" data-trxid="{{$item->trx_id}}" data-kodetrx="{{$item->Transaksi->kode_trx}}" data-nama="{{$item->Transaksi->peg_nama}}" data-nip="{{$item->Transaksi->peg_nip}}" data-tujuan="{{$item->Transaksi->Matrik->Tujuan->nama_kabkota}}" data-tugas="{{$item->Transaksi->tugas}}" data-tglsurat="@if ($item->tgl_surat) {{Tanggal::Panjang($item->tgl_surat)}} @endif" data-tglbrkt="@if ($item->Transaksi->tgl_brkt) {{Tanggal::Panjang($item->Transaksi->tgl_brkt)}} @endif" data-tglbalik="@if ($item->Transaksi->tgl_balik) {{Tanggal::Panjang($item->Transaksi->tgl_balik)}} @endif" data-nomor="{{$item->nomor_surat}}" data-ttd="{{$item->ttd_jabatan}}-{{$item->ttd_nama}}" data-status="{{$FlagSrt[$item->flag_surattugas]}}" data-lamanya="{{$item->Transaksi->bnyk_hari}} ({{$Bilangan[$item->Transaksi->bnyk_hari]}}) Hari">{{$item->Transaksi->kode_trx}}</a>
