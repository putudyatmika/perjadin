@if ($item->tgl_surat!=NULL and ($item->flag_surattugas!=3 and $item->flag_surattugas!=0) )
<a href="{{route('surattugas.view',$item->Transaksi->kode_trx)}}" class="btn btn-circle btn-primary btn-sm"><span data-toggle="tooltip" title="Cetak surat tugas an. {{$item->Transaksi->peg_nama}}"><i class="fa fa-print"></i></span></a>
@endif
@if (($item->flag_surattugas<2 and Auth::user()->pengelola>3) or Auth::user()->pengelola==5)
<button type="button" class="btn btn-success btn-circle btn-sm" data-toggle="modal" data-target="#EditModal" data-srtid="{{$item->srt_id}}" data-trxid="{{$item->trx_id}}" data-kodetrx="{{$item->Transaksi->kode_trx}}" data-nama="{{$item->Transaksi->peg_nama}}" data-tujuan="{{$item->Transaksi->Matrik->Tujuan->nama_kabkota}}" data-tugas="{{$item->Transaksi->tugas}}" data-nomor="{{$item->nomor_surat}}" data-ttd="{{$item->flag_ttd}}" data-ttdnip="{{$item->ttd_nip}}" data-tglsurat="{{$item->tgl_surat}}" data-tglbrkt="{{Tanggal::Panjang($item->Transaksi->tgl_brkt)}}" data-tglbalik="{{$item->Transaksi->tgl_brkt}}" data-tahun="{{Session::get('tahun_anggaran')}}"><span data-toggle="tooltip" title="Edit surat tugas an. {{$item->Transaksi->peg_nama}}"><i class="fa fa-pencil"></i></span></button>
<button type="button" class="btn btn-danger btn-circle btn-sm" data-toggle="modal" data-target="#BatalModal" data-srtid="{{$item->srt_id}}" data-trxid="{{$item->trx_id}}" data-kodetrx="{{$item->Transaksi->kode_trx}}" data-nama="{{$item->Transaksi->peg_nama}}" data-tujuan="{{$item->Transaksi->Matrik->Tujuan->nama_kabkota}}" data-tugas="{{$item->Transaksi->tugas}}" data-tglsurat="{{$item->tgl_surat}}" data-tglbrkt="{{$item->Transaksi->tgl_brkt}}" data-tglbalik="{{$item->Transaksi->tgl_balik}}" data-tid="{{$item->Transaksi->Matrik->dana_tid}}" data-pagu_rencana="{{$item->Transaksi->Matrik->total_biaya}}"><span data-toggle="tooltip" title="Batalkan surat tugas an. {{$item->Transaksi->peg_nama}}"><i class="fa fa-times"></i></span></button>
@endif

